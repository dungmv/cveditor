import contentEditable from './Editable'

const H1Editable = contentEditable('h1');
const H5Editable = contentEditable('h5');
const PEditable = contentEditable('p');
const DivEditable = contentEditable('div');
const AEditable = contentEditable('a');

export {
    H1Editable,
    H5Editable,
    PEditable,
    DivEditable,
    AEditable,
}
